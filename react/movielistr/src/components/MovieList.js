import React, { Component } from 'react'
import MovieCard from './MovieCard'

class MovieList extends React.Component {
  render() {
    // const movieItems = this.props.movies.map((movie) => <MovieCard movie={movie}/>)
    return (
      <div>
        
      </div>
    )
  }
}


export default MovieList
